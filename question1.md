Write a github Actions workflow that:
- Triggers on every push to the main branch
- installs Node.js version 20
- Runs npm install
- Runs npm test


For this to work create a simple expressjs server that renders a simple text in order to test
